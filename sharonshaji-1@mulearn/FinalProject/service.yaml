apiVersion: v1
kind: Service
metadata:
  name: docker-x-k8s-service
spec:
  type: NodePort
  selector:
    app: simple-web-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
